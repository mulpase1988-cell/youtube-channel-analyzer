name: 📌 Step 1 - 채널ID 추출

on:
  workflow_dispatch:
    inputs:
      range:
        description: '처리 범위 (예: 1,101)'
        required: false
        default: '1,10'
        type: string

jobs:
  step1:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'

      - run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - run: mkdir -p data

      - name: Step 1 실행
        env:
          GOOGLE_SERVICE_ACCOUNT: ${{ secrets.GOOGLE_SERVICE_ACCOUNT }}
          SHEET_NAME: '유튜브보물창고_테스트'
          DATA_TAB_NAME: '데이터'
          RANGE: ${{ github.event.inputs.range || '1,10' }}
        run: python scripts/step1_extract_channel_ids.py

      - if: always()
        run: |
          if [ -f data/channel_ids.json ]; then
            echo "✅ channel_ids.json 생성됨"
            head -30 data/channel_ids.json
          fi

      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: step1-results
          path: data/channel_ids.json
          retention-days: 30
